# public/admin/config.yml
backend:
  name: git_gateway
  branch: main

publish_mode: simple    # ← 여기!

media_folder: "public/uploads"
public_folder: "/uploads"

collections:
  - name: "stories"
    label: "동행이야기"
    folder: "src/content/stories"
    create: true
    format: "frontmatter"
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "제목", name: "title", widget: "string" }
      - { label: "작성일", name: "date", widget: "datetime", format: "YYYY-MM-DDTHH:mm:ssZ" }
      - { label: "대표 이미지(선택)", name: "thumbnail", widget: "image", choose_url: true, required: false }
      - label: "카테고리"
        name: "category"
        widget: "select"
        options: ["인터뷰", "행사", "공탁", "공조동방"]
        default: "인터뷰"
      - { label: "본문", name: "body", widget: "markdown" }

  - name: "notices"
    label: "공지/공모"
    folder: "src/content/notices"
    create: true
    format: "frontmatter"
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "제목", name: "title", widget: "string" }
      - { label: "작성일", name: "date", widget: "datetime", format: "YYYY-MM-DDTHH:mm:ssZ" }
      - { label: "대표 이미지(선택)", name: "thumbnail", widget: "image", choose_url: true, required: false }
      - { label: "본문", name: "body", widget: "markdown" }